/**
 * Blank layout for /studio — no Aiva navbar/footer, just the full-screen studio.
 */
export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
